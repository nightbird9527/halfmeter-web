import React from 'react';
import { Layout, Menu, ConfigProvider, Button } from 'antd';

const { Header } = Layout;

const HeaderBar = () => {
    return (
        <Header style={{ backgroundColor: '#333' }}>Header</Header>
    )
}

export default HeaderBar;